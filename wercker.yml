box: flenter/android@0.0.11-rc1
build:
  steps:
    - script:
        name: show info
        code: |
          export

    - script:
        name: run gradle
        code: |
          gradle -v
          gradle --refresh-dependencies --rerun-tasks --full-stacktrace build

    - script:
        name: show build result
        code: |
          ls -la Hello/build/apk
